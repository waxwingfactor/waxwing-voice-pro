{
  "name": "waxwing-voice-pro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:api",
    "build:api": "npm run build --workspace @waxwing/core && npm run build --workspace @waxwing/db && npm run build --workspace @waxwing/api",
    "build:all": "npm run build:api && npm run build:web",
    "build:web": "npm run build --workspace @waxwing/web",
    "dev": "npm run dev --workspace @waxwing/api",
    "dev:web": "npm run dev --workspace @waxwing/web",
    "start": "npm run start --workspace @waxwing/api",
    "start:web": "npm run start --workspace @waxwing/web",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
